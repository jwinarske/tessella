//! Tables generated from maplibre-native (DR-6).
//!
//! Never hand-edited. `cargo run -p mbgl-codegen -- --mbgl <tree>` rewrites them, and
//! `--check` fails when they have drifted from the pinned tree.

#[cfg(feature = "collator")]
pub mod collation;
pub mod operators;
