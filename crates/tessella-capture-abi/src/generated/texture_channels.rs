//! How many channels a texture pixel format carries, generated from
//! maplibre-native.
//!
//! Source revision: b5a2922844c9
//!
//! From `Texture2DDesc::channelCount`. The enum alone says which formats
//! exist and not how large a pixel of each is, which is enough for a
//! producer — every caller sizes its own slice — and not enough for a
//! consumer: a whole-texture upload's byte count can only be checked
//! against its extent by something that knows this.
//!
//! Generated by `cargo run -p mbgl-codegen`. Do not edit.

use crate::generated::mbgl_enums::TexturePixelType;

impl TexturePixelType {
    /// How many channels one pixel of this format carries.
    ///
    /// Every channel is one byte on this stream: mbgl's
    /// `channelStorageSize` is per *channel data type*, and nothing here
    /// sends anything but unsigned bytes.
    #[must_use]
    pub const fn channels(self) -> u32 {
        match self {
            Self::Alpha => 1,
            Self::Depth => 1,
            Self::Luminance => 1,
            Self::Stencil => 1,
            Self::RGBA => 4,
        }
    }
}
