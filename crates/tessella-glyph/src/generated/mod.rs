//! Tables generated from the pinned maplibre-native tree (DR-6).
//!
//! Regenerated by `cargo run -p mbgl-codegen -- --mbgl <tree>`, and checked in CI with
//! `--check`. Hand-editing anything here is how a table drifts from the engine it mirrors, and
//! the drift is silent: the symptom is a map that renders slightly differently, not a build
//! that fails.

pub mod arabic;
pub mod blocks;
